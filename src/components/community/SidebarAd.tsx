import { SponsorCard } from "@/components/ui/SponsorCard";

/** The Community sidebar's sponsor slot — empty until a sponsor is booked for it. */
export function SidebarAd() {
  return <SponsorCard placement="COMMUNITY" />;
}
